import { NextResponse } from 'next/server';import { getAdminSupabase } from '@/lib/supabase';import { requireSession } from '@/lib/auth'
export async function GET(){const user=await requireSession();if(!user)return NextResponse.json({error:'Chưa đăng nhập.',items:[]},{status:401});const db=getAdminSupabase();if(!db)return NextResponse.json({configured:false,items:[]});const{data,error}=await db.from('brain_backups').select('id,label,created_at').eq('user_id',user.id).order('created_at',{ascending:false}).limit(30);if(error)return NextResponse.json({error:error.message,items:[]},{status:500});return NextResponse.json({configured:true,items:data||[]})}
export async function POST(req:Request){const user=await requireSession();if(!user)return NextResponse.json({error:'Chưa đăng nhập.'},{status:401});const db=getAdminSupabase();if(!db)return NextResponse.json({error:'Supabase chưa được cấu hình.'},{status:503});const b=await req.json().catch(()=>({}));const{data:knowledge,error:kerr}=await db.from('knowledge').select('*').eq('user_id',user.id).order('created_at',{ascending:true});if(kerr)return NextResponse.json({error:kerr.message},{status:500});const{data:quickNotes}=await db.from('quick_notes').select('*').eq('user_id',user.id).order('created_at',{ascending:true});const{data,error}=await db.from('brain_backups').insert({user_id:user.id,label:String(b.label||'Manual backup'),payload:{knowledge:knowledge||[],quick_notes:quickNotes||[]}}).select('id,label,created_at').single();if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json({ok:true,backup:data,count:knowledge?.length||0})}
export async function PUT(req:Request){const user=await requireSession();if(!user)return NextResponse.json({error:'Chưa đăng nhập.'},{status:401});const db=getAdminSupabase();if(!db)return NextResponse.json({error:'Supabase chưa được cấu hình.'},{status:503});const{id}=await req.json();if(!id)return NextResponse.json({error:'Thiếu backup id.'},{status:400});const{data,error}=await db.from('brain_backups').select('payload').eq('id',id).eq('user_id',user.id).single();if(error)return NextResponse.json({error:error.message},{status:500});const rows=Array.isArray(data?.payload?.knowledge)?data.payload.knowledge:[];await db.from('knowledge').delete().eq('user_id',user.id);if(rows.length){const clean=rows.map((x:any)=>({...x,user_id:user.id}));const{error:insErr}=await db.from('knowledge').insert(clean);if(insErr)return NextResponse.json({error:insErr.message},{status:500})}const quickRows=Array.isArray(data?.payload?.quick_notes)?data.payload.quick_notes:[];await db.from('quick_notes').delete().eq('user_id',user.id);if(quickRows.length)await db.from('quick_notes').insert(quickRows.map((x:any)=>({...x,user_id:user.id})));return NextResponse.json({ok:true,count:rows.length,quickNotes:quickRows.length})}
