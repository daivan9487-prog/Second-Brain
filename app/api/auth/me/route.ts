import { NextResponse } from 'next/server';import { requireSession } from '@/lib/auth';export async function GET(){const user=await requireSession();return NextResponse.json({authenticated:!!user,user})}
