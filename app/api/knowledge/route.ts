import { NextResponse } from 'next/server'
import { getAdminSupabase } from '@/lib/supabase'
import { createEmbeddingWithSettings } from '@/lib/ai'
import { loadUserAISettings } from '@/lib/ai-vault'
import { requireSession } from '@/lib/auth'
export async function GET(){const user=await requireSession();if(!user)return NextResponse.json({error:'Chưa đăng nhập.',items:[]},{status:401});const db=getAdminSupabase();if(!db)return NextResponse.json({configured:false,items:[]});const{data,error}=await db.from('knowledge').select('*').eq('user_id',user.id).order('created_at',{ascending:false}).limit(1000);if(error)return NextResponse.json({error:error.message},{status:500});return NextResponse.json({configured:true,items:data||[]})}
export async function POST(req:Request){const user=await requireSession();if(!user)return NextResponse.json({error:'Chưa đăng nhập.'},{status:401});const db=getAdminSupabase();if(!db)return NextResponse.json({error:'Supabase chưa được cấu hình.'},{status:503});const body=await req.json() as {title?:string,content?:string,category?:string,topic?:string,tags?:string[],sourceUrl?:string};const rawTitle=String(body.title||'').trim(),content=String(body.content||'').trim(),category=String(body.category||'General').trim()||'General';if(!rawTitle&&!content)return NextResponse.json({error:'Hãy nhập tiêu đề hoặc nội dung.'},{status:400});const title=rawTitle||content.slice(0,80).trim()||'Kiến thức không tiêu đề',topic=String(body.topic||'').trim(),tags=Array.isArray(body.tags)?body.tags.map(String).map(x=>x.trim()).filter(Boolean):[],source_url=String(body.sourceUrl||'').trim()||null;const insert:any={user_id:user.id,title,content,category,topic:topic||null,tags,source_url};let{data:item,error}=await db.from('knowledge').insert(insert).select().single();if(error&&/topic|tags/i.test(error.message)){const r=await db.from('knowledge').insert({user_id:user.id,title,content,category,source_url,metadata:{topic,tags}}).select().single();item=r.data;error=r.error}if(error)return NextResponse.json({error:error.message},{status:500});try{const aiSettings=await loadUserAISettings(user.id);const embedding=await createEmbeddingWithSettings(`${category}\n${topic}\n${title}\n${tags.join(' ')}\n${content}`,aiSettings);if(embedding)await db.from('knowledge_chunks').insert({knowledge_id:item.id,chunk_text:content||title,embedding})}catch{}return NextResponse.json({item})}
